import styles from './backLearn.module.scss'

export const BackLearn = () => {
	return (
		<section className={styles.backLearn}>
			<h1 className={styles.title}>В разработке....</h1>
		</section>
	)
}
